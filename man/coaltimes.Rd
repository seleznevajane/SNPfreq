% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R, R/documentation.R
\name{coaltimes}
\alias{coaltimes}
\title{Expectations of Coalescence Times}
\usage{
coaltimes(
  n,
  population_type = 0L,
  kappa = 1,
  Ne0 = 100L,
  T = 10L,
  NeT = 10L,
  beta = 0.5,
  tk = as.numeric(c()),
  eta = as.numeric(c())
)

coaltimes(n, population_type = 0)
coaltimes(n, population_type = 1, kappa)
coaltimes(n, population_type = 2, Ne0, NeT, T)
coaltimes(n, population_type = 3, kappa, beta)
coaltimes(n, population_type = 4, tk, eta)
}
\arguments{
\item{n}{Sample size.}

\item{population_type}{Integer code for the population model:
\itemize{
\item \code{0}: Constant.
\item \code{1}: Exponential (requires \code{kappa}).
\item \code{2}: Biphasic (requires \code{Ne0}, \code{NeT}, and \code{T}).
\item \code{3}: Power (requires \code{kappa} and \code{beta}).
\item \code{4}: Stepwise Constant (requires \code{tk} and \code{eta}).
}}

\item{kappa}{Product of current effective population size and growth rate,
\code{kappa = Ne0 * r} (Exponential, Power models).}

\item{Ne0}{Current effective population size (Biphasic model).}

\item{T}{Time in generations when the model changed (Biphasic model).}

\item{NeT}{Effective population size at moment T (Biphasic model).}

\item{beta}{Growth equation power (Power model). If \code{beta < 0}, increasing
population; if \verb{0 < beta < 1}, decreasing population.}

\item{tk}{Vector of times of population changes (Stepwise Constant model). See details.}

\item{eta}{Vector of effective population sizes for intervals in \code{tk}
(Stepwise Constant model). See details.}
}
\value{
A numeric vector of expected coalescence times.
}
\description{
Computes a vector of expected coalescence times up to a multiplier for
various population growth models.
}
\details{
We consider the process of coalescence with time-changing effective population
size \eqn{N_e(t)}. For a sample size \eqn{n}, the function computes expectations
of coalescence times:
\deqn{e_j = \int_0^\infty t \, q_j(t) \, dt}
where the distribution \eqn{q_j(t)} is defined as:
\deqn{q_j(t) = \frac{\binom{j}{2}}{N_e(t)} \exp\left( -\int_0^t \frac{\binom{j}{2} d\sigma}{N_e(\sigma)} \right)}

The following population models are supported:
\itemize{
\item \strong{Constant:} \eqn{N_e(t) = N_{e0}}
\item \strong{Exponential:} \eqn{N_e(t) = N_{e0} \exp(-rt)}
\item \strong{Biphasic (constant, then exponential):}
\deqn{N_e(t) = \begin{cases} N_{e0} \exp(-rt), & 0 \le t \le T \\ N_{e0} \exp(-rT), & t > T \end{cases}}
\item \strong{Power:} \eqn{N_e(t) = N_{e0}(1+rt)^\beta}
\item \strong{Stepwise Constant:} #'   \deqn{N_e(t) = \begin{cases} \eta_1, \quad & 0 \le t < t_1 \\
                               \eta_2, \quad & t_1 \le t < t_2 \\ \vdots \\
                               \eta_K = 0, \quad & t \ge t_{K-1} \end{cases}}
}

For stepwise constant model, you must provide vectors \code{tk} = \eqn{(0, t_1,\dots, t_{K-1})}
and \code{eta} = \eqn{(\eta_1,\dots, \eta_K)}.

This function is designed to be used in conjunction with \code{\link[=SNPprob]{SNPprob()}}
and computes expected times only up to a multiplier.
}
\examples{
library(SNPfreq)
n <- 30

# Constant population model
e_const <- coaltimes(n, population_type = 0)

# Exponential population model
e_exp <- coaltimes(n, population_type = 1, kappa = 10)

# Biphasic population model
e_biph <- coaltimes(n, population_type = 2, Ne0 = 10, NeT = 1, T = 1)

# Power population model
e_power <- coaltimes(n, population_type = 3, kappa = 1, beta = 0.5)

# Stepwise constant population model
e_step <- coaltimes(n, population_type = 4, 
                    tk = c(0, 9, 100, 600, 10000, 290000), 
                    eta = c(1e8, 1e6, 1e5, 4000, 5000, 0))

}
\references{
Polanski, A., & Kimmel, M. (2003). New explicit expressions for relative
frequencies of single-nucleotide polymorphisms with application to
statistical inference on population growth. Genetics, 165(1), 427-436.
\url{https://pmc.ncbi.nlm.nih.gov/articles/PMC1462751/}
}
\seealso{
\code{\link[=SNPprob]{SNPprob()}}
}
